/* exported getFirstChar */

/* the function should have a string with a paramater of a string.
next step is should stroe strings.
then it should choose the first latter of a word.
after it stores and chose the first latter of a string.
it should give us an output of the first latter of a word.
[x]creat a fucntion that stroes a string
[x]give a name to that funtion
[x]that function should chose the first latter of a word and then return an output.
*/

function getFirstChar(string) {
  return string[0];
}
