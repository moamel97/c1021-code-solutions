/* exported getFirstChar */
/* we need to get the first letter of a string the input is a word and we need to extract and return the first letter of the word
input = LearningFuze
output = L
step 1 - we create a function that takes in a string as parameter
step 2 - we create a variable that holds the first letter of the string
step 3 - we return that variable
*/

function getFirstChar(string) {
  const firstLetter = string[0];
  return firstLetter;
}
