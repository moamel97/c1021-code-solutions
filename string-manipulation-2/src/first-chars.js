/* exported firstChars */
/* step 1 create a function thats called firstChars
step 2 give the string 2 parameters length and string
step 3 use the substring method to loop over the string length starting at 0 and stopping point is the length
step 4 return the substring */

function firstChars(length, string) {
  return string.substring(0, length);
}
