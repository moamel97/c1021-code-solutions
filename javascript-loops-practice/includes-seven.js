/* exported includesSeven */

function includesSeven(array) {
  var input = [];
  for (var i = 0; i < array.length; i++) {
    if(array[i] === 7)
    return true;
  } {
    return false;
  }
  input.push(array.length[i])

}
