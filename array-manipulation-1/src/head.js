/* exported head */
/* we need to extract the first element of the array.

step 1 create a function and give it array as a parameter
step 2 create a conditional that checks if the array is an empty array and if its then return undifined
step 3 return the array at the first position.
*/

function head(array) {
  return array[0];
}
