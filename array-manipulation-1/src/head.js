/* exported head */

/* create a function that loops through an array of words then picks the first index of a word and returns that word */

function head(array) {
  for(var i = 0; i < array.length; i++) {
    var firstIndex = array[0];
  }
  return firstIndex;
}
