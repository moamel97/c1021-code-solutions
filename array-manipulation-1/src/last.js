/* exported last */

/* first create a function that checks for the last index of an array
and then returns that last word */

function last(array) {
  for(var i = 0 ; i < array.length ; i++) {
    var lastIndex = array[i];
  }
  return lastIndex;
}
